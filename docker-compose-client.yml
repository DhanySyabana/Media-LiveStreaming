version: '3'

services:
  monitor_server_livestream:
    build: 
      context: .
      dockerfile: client.Dockerfile
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Jakarta
    command: python Client.py